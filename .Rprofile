.libPaths(c(".packages", .libPaths()))
options(menu.graphics=FALSE)

.First <- function(){

  cat("Make sure you have '.packages' folder.")
  cat("Lib path loaded:\n")
  for (i in .libPaths()) {
    cat("  ", i, "\n")

  }
}